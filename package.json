{
  "name": "rahoot",
  "scripts": {
    "dev": "dotenv -e .env -- pnpm --filter web dev",
    "dev:web": "dotenv -e .env -- pnpm --filter web dev",
    "build": "pnpm -r run build",
    "start": "pnpm --filter web start",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.9.3"
  }
}
